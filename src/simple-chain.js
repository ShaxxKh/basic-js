const CustomError = require("../extensions/custom-error");
/* let res = [] */
const chainMaker = {
  /* res: [],

  getLength() {
    chainMaker.res.length()
  },
  addLink(value) {
    res.push(value)
  },
  removeLink(position) {
    res.slice(position, position)
  },
  reverseChain() {
    res.reverse()
  },
  finishChain() {
    return chainMaker.res
  }
 */  
};

/* console.log(chainMaker.getLength().finishChain()) */
module.exports = chainMaker;
